import React from 'react'
import ReactDOM from 'react-dom/client'
import EnterProfile from './src/EnterProfile'
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EnterProfile />
  </React.StrictMode>,
)
